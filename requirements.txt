# --- API / serving ---
fastapi==0.140.0
uvicorn[standard]=0.51.0
pydantic==2.13.4

# --- Dashboard ---
streamlit==1.60.0

# --- Orchestration ---
dagster==1.13.15
dagster-webserver==1.13.15

# --- Experiment tracking / registry / versioning ---
mlflow==3.14.0
dagshub==0.7.1
dvc==3.67.1
dvc-s3==3.3.0

# --- Data validation / drift ---
pandera==0.32.1
evidently==0.7.21

# --- Classical ML + ONNX inference ---
scikit-learn==1.9.0
onnxruntime==1.28.0
skl2onnx==1.20.0
optimum[onnxruntime]==2.1.0
onnx==1.22.0

# --- Tokenization for DistilBERT ONNX inference (no torch needed locally) ---
transformers==4.57.6
tokenizers==0.22.2

# --- LLM fallback / explanation / PII clients ---
groq==1.6.0
openai==2.48.0

# --- Database ---
sqlalchemy==2.0.51
psycopg2-binary==2.9.12

# --- Observability ---
logfire[fastapi]==4.39.0

# --- Utilities ---
python-dotenv==1.2.2
pandas==2.3.3
numpy==2.4.6
kaggle==2.2.4
requests==2.34.2

# --- Testing / lint ---
pytest==9.1.1
ruff==0.16.0